<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.gehrung.beerpongxtreme.Parcours"
    android:background="@drawable/tisch">

    <ImageView
        android:id="@+id/parcourslogo"
        android:layout_width="fill_parent"
        android:layout_height="150dp"
        android:layout_margin="5dp"
        android:src="@drawable/parcourslogo" />

    <ImageButton
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:id="@+id/highscore"
        android:src="@drawable/highscorebutton"
        android:scaleType="fitCenter"
        android:backgroundTint="@android:color/transparent"
        android:layout_marginBottom="95dp"
        android:layout_above="@+id/hilfe"
        android:layout_alignLeft="@+id/play"
        android:layout_alignStart="@+id/play"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:id="@+id/musik"
        android:src="@drawable/musik_off"
        android:scaleType="fitCenter"
        android:backgroundTint="@android:color/transparent"
        android:layout_below="@+id/highscore"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/hilfe"
        android:src="@drawable/hilfe"
        android:scaleType="fitCenter"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@android:color/transparent" />

    <ImageButton
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/home"
        android:src="@drawable/home"
        android:scaleType="fitCenter"
        android:layout_below="@+id/highscore"
        android:layout_alignRight="@+id/parcourslogo"
        android:layout_alignEnd="@+id/parcourslogo"
        android:backgroundTint="@android:color/transparent" />

    <ImageButton
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:id="@+id/play"
        android:src="@drawable/playbutton"
        android:scaleType="fitCenter"
        android:backgroundTint="@android:color/transparent"
        android:layout_below="@+id/parcourslogo"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
